---
import Layout from '../layouts/Layout.astro';
export interface Props {
  src: string;
  alt?: string;
}

const { src, alt = "" } = Astro.props;
---

<div class="fixed inset-0">
  <img src={src} alt={alt} class="w-screen h-screen object-fill" />
</div>
